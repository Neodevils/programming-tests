const superPeople = []; // This is an array of strings
const peoplePower = []; // Array<number> is the same as number[]
const allPeople = []; // This is an array of Person objects
const Models = [[366], [33]]; // This is an array of arrays of numbers
superPeople.push("Superman");
peoplePower.push(100);
allPeople.push({ name: "Superman", isActivated: true });
export {};
